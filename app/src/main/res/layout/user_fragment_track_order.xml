<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimaryDark"
    tools:context=".ui.userUI.trackOrder.TrackOrderFragment">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/track_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="@dimen/_5sdp"
        tools:listitem="@layout/user_item_order_receipt_layout" />

    <Button
        android:id="@+id/showSummary"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginEnd="@dimen/_16sdp"
        android:layout_marginBottom="@dimen/_16sdp"
        android:background="@drawable/rounded_button_shap"
        android:paddingLeft="20dp"
        android:paddingRight="20dp"
        android:text="@string/show_summary"
        android:textColor="#ffffff" />

</RelativeLayout>